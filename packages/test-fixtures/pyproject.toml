[project]
name = "soa-fixtures"
version = "0.1.0"
description = "Deterministic demo tenant fixtures and seed runner"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/soa_fixtures"]
