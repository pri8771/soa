[project]
name = "soa-rules"
version = "0.1.0"
description = "Deterministic, bounded, traced rule evaluation over extracted document values"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/soa_rules"]
