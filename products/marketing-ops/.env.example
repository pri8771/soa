# Product runtime
NODE_ENV=development
MARKETING_OPS_ENV=local

# Web
WEB_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:4100

# API
API_HOST=0.0.0.0
API_PORT=4100

# Worker
WORKER_HOST=0.0.0.0
WORKER_PORT=4101
WORKER_POLL_INTERVAL_MS=1000

# Provider composition placeholders (implemented in later backlog tasks)
SOCIAL_PROVIDER=mock
AI_PROVIDER=mock
DATABASE_URL=postgresql://marketing_ops:marketing_ops@localhost:5433/marketing_ops
OBJECT_STORE_ENDPOINT=http://localhost:9000
